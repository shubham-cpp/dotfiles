#!/bin/sh

set -e

copy="xclip -i -r -sel clip"

if [ "$XDG_SESSION_TYPE" = "wayland" ]; then
  copy="wl-copy"
fi

fc-list : family | tr ',' '\n' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | sort -fu | sk | xargs -r $copy
